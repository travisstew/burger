
<div class="container">
  <div class="row">
    <div class="col-sm">
       
       <ul class="list-group">
         {{#each burger}}
        <li id="{{this.id}}" class="list-group-item">{{this.id}} {{this.burger_name}}  <button type="button" class="btn btn-primary btn-sm float-right">Devoured</button></li>
          {{/each}}
       </ul>
    </div>
    
      <div class="col-sm">

        <form id="burger-form">
          <div class="form-group">
          <label for="exampleFormControlTextarea1">Example textarea</label>
          <textarea class="form-control" id="burger-input" rows="3" ></textarea>
          </div>
          <button  type="submit" class="btn btn-primary mb-2">Confirm identity</button>
        </form>

      </div>
        
    </form>
    <div class="col-sm">
      One of three columns
    </div>
  </div>
</div>